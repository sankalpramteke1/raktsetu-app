# RaktSetu Mobile Application
**Durg District Blood Center — District Hospital, Durg, Chhattisgarh**

## Running the App
```bash
npm install
npx expo start
```

Press `a` for Android, `i` for iOS, `w` for Web.
